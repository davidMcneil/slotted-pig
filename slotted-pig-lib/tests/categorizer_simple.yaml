categories:
  - category: expenses
    children: !subcategories
      - category: store
        children: !transaction_matchers
          - description: "store.*"
  - category: income
    children: !subcategories
      - category: paycheck
        children: !transaction_matchers
          - min: 0
